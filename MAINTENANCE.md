# 后期代码维护策略

更新时间：2026-06-23

## 维护目标

保证小红书运营系统长期可运行、可排查、可扩展，并且不会因为 API 密钥、数据丢失或模型变更影响日常运营。

## 代码管理

- `main` 分支只保留可运行版本。
- 新功能用独立分支开发，分支名建议使用 `codex/功能名称`。
- 每次提交只包含一个明确目标，例如“新增图片生成节点”或“修复日报 PDF 渲染”。
- 不把 API Key、Webhook、数据库、微信聊天记录、真实客户资料提交到 GitHub。
- 配置模板放在 `config/settings.example.toml`，真实配置放在 `config/settings.toml` 或服务器环境变量。

## API 和密钥

- 所有密钥从配置文件或环境变量读取。
- 每个外部 API 都需要记录失败原因，避免静默失败。
- 更换模型时优先改配置，不直接改业务流程。
- 图片生成、大模型、小红书数据 API 分开配置，避免一个服务异常拖垮全部流程。

## 数据维护

- 业务数据优先写入数据库，不长期依赖零散表格。
- 笔记指标保留历史快照，用于后续判断增长曲线。
- 生成内容、图片任务、图片结果、模型调用日志都要保留。
- 云端数据库至少每日备份一次。
- 图片和报告产物定期备份；临时目录 `tmp/` 不做长期保存。

## 日常检查

每天检查：

- 定时任务是否执行。
- 昨日是否有新笔记入库。
- 日报是否生成。
- 图片任务是否有失败。
- 企业微信/通知是否发送成功。

每周检查：

- 周报是否生成。
- 高互动笔记是否被正确识别。
- 三个业务号内容是否区分明显。
- API 调用成本是否异常。
- 数据库文件或云端存储是否接近容量上限。

## Bug 处理流程

1. 记录 bug：写清楚发现人、日期、功能、复现步骤、截图或证据。
2. 判断优先级：影响系统运行、数据丢失、API 失败为高优先级。
3. 修复：只改和 bug 直接相关的代码。
4. 验证：重新跑对应命令或测试用例。
5. 记录结果：说明修复方式和验证结果。

## 路演反馈处理

- 第一次路演建议统一记录到 `docs/roadshow-feedback.md`。
- 每条建议标记：采纳、部分采纳、暂不采纳。
- 采纳建议必须有对应代码、文档或配置修改。
- 暂不采纳建议必须写明原因，例如成本高、当前阶段不影响验收、依赖外部 API。

## 部署维护

- 云端部署前先在本地跑通最小闭环。
- 云端使用独立配置，不复用本机密钥文件。
- 定时任务失败需要有日志。
- 服务器更新前先备份数据库。
- 更新后至少验证：同步、日报、图片节点、通知。

## 模型维护

- 普通清洗和字段提取优先用规则或低成本模型。
- 爆贴深度分析、业务号生成、封面提示词使用高级模型。
- 图片生成/改图使用独立图片模型。
- 每次模型调用后续需要记录：任务类型、模型、耗时、是否成功、错误信息。

## 数据安全

- 真实账号、客户信息、聊天记录默认不公开。
- 展示给路演的样例需要脱敏。
- GitHub 仓库公开前必须检查配置文件、CSV、文档中是否含密钥或隐私。
- 离职或换人维护时，需要轮换 API Key 和服务器密码。
